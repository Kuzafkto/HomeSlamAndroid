<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TfgProject" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
        <item name="colorPrimary">@color/green_background</item>

    </style>
    <style name="ShapeAppearanceOverlay.App.Circle" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>
    <style name="BlackTextInputLayout" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox.Dense">
        <item name="boxStrokeColor">@color/black</item> <!-- Borde del TextInputLayout -->
        <item name="hintTextColor">@color/black</item> <!-- Color del texto del hint -->
        <item name="android:textColorHint">@color/black</item> <!-- Color del hint cuando el EditText no está enfocado -->
        <item name="android:textColor">@color/black</item> <!-- Color del texto introducido -->
        <item name="colorOnSurface">@color/black</item> <!-- Usado para el color del label flotante cuando está elevado -->
        <item name="endIconTint">@color/black</item> <!-- Color del ícono al final si existe -->
    </style>
</resources>